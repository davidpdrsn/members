<% if @members.present? %>
  <ul>
    <% @members.each do |member| %>
      <li>
        <%= link_to member.name, member %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>There are no members yet 😭</p>
<% end %>
